rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily allow read/write access to all collections for testing/demo purposes
      allow read, write: if true;
    }
  }
}
